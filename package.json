{
  "name": "code-hive",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "bin": {
    "hive": "./cli/index.ts"
  },
  "scripts": {
    "dev": "prisma generate && prisma db push && tsx server/index.ts",
    "dev:mcp": "prisma generate && prisma db push && tsx mcp/server.ts",
    "build": "prisma generate && tsc -p tsconfig.json",
    "test": "prisma generate && vitest run --passWithNoTests",
    "db:push": "prisma db push",
    "prisma:generate": "prisma generate"
  },
  "dependencies": {
    "@clack/prompts": "^1.5.1",
    "@fastify/multipart": "^10.0.0",
    "@fastify/static": "^9.1.3",
    "@fastify/websocket": "^11.2.0",
    "@modelcontextprotocol/sdk": "^1.13.0",
    "@prisma/client": "^6.10.0",
    "fastify": "^5.3.0",
    "zod": "^3.25.0"
  },
  "devDependencies": {
    "@types/node": "^22.15.0",
    "prisma": "^6.10.0",
    "tsx": "^4.19.0",
    "typescript": "^5.8.3",
    "vitest": "^3.2.0"
  }
}
